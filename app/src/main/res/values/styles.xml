<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Botón principal: teal, redondeado -->
    <style name="Widget.TripPlanner.Button.Primary" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>

    <!-- Botón secundario: contorno naranja -->
    <style name="Widget.TripPlanner.Button.Secondary" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="cornerRadius">12dp</item>
        <item name="strokeColor">@color/accent_orange</item>
        <item name="android:textColor">@color/accent_orange</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>
</resources>
